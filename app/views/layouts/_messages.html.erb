<% flash.each do |name, msg| %>
    <div class="flash-message sticky">
      <% if msg.is_a?(String) %>
          <%= content_tag :div, msg, :class => "flash_#{name}" %>
      <% end %>
    </div>
<% end %>